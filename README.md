# Detection-of-Cyberbulling
This project builds a machine-learning model to detect cyberbullying in tweets. It combines scraped and public datasets, cleans and preprocesses text, uses TF-IDF for feature extraction, and trains classification models to identify harmful or abusive language with high accuracy.
